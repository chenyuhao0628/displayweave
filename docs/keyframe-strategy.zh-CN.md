# 关键帧策略

周期 GOP 候选默认值为 WiFi 两秒、USB 一秒：减少 WiFi 周期性突发，同时保留有线连接的快速恢复。接收端请求、重连、codec 失败/回退、队列丢帧、编码器或串流重配置仍会立即请求关键帧。

Debug 编码统计会记录关键帧数、平均关键帧大小、峰值帧大小和窗口结束时的队列深度。实体测试必须比较 WiFi 1/2/3 秒与 USB 1/2 秒 GOP，同时观察队列/Frame Age 峰值和解码恢复时间。

完成并记录这些测试前，当前默认值仍属于候选值。
